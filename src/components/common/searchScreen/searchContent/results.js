import { View, Text } from "tamagui";

export const Results = () => {
    return (
        <View f={1} justifyContent="center" alignItems="center">
            <Text>Results</Text>
        </View>
    );
};