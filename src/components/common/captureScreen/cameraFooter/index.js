import { View } from "tamagui";
import CaptureButton from "./captureButton";

const CaptureCameraFooter = () => {
	return (
		<View>
			<CaptureButton />
		</View>
	);
};

export default CaptureCameraFooter;
