import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	containerBase: {
		position: "relative",
		overflow: "hidden",
	},
	absoluteFill: {
		position: "absolute",
		top: 0,
		left: 0,
		right: 0,
		bottom: 0,
		width: "100%",
		height: "100%",
	},
});
